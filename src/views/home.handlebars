<title>Lista de Productos</title>

<body>
  <h1>Lista de Productos</h1>

  <table class="table table-hover">
    <thead>
      <tr>
        <th>Nombre</th>
        <th>Descripción</th>
        <th>Precio</th>
        <th>Stock</th>
        <th>Imagen</th>
      </tr>
    </thead>



     <tbody>
  {{#each productsPlain}}
    <tr>
      <td>{{title}}</td>
      <td>{{price}}</td>
      <td>{{description}}</td>
      <td><img src="{{thumbnail}}" class="img-thumbnail" alt="thumbnail" /></td>
      <td>{{code}}</td>
      <td>{{stock}}</td>
      <td>{{quantity}}</td>
    </tr>
  {{/each}}
</tbody>


  </table>
  <button class="btn btn-primary" onclick="{goProducts()}">Go realTimeProducts</button>

</body>
<script>
  function goProducts() { window.location.href = '/realTimeProducts'; }
</script>